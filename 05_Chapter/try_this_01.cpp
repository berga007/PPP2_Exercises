// This program runs the 1st try this from chapter 05
#include "../std_lib_facilities.h"

int main()
{
    // Declaring the function to be called
    int area(int lenght, int width);

    int x2 = area(1);
}